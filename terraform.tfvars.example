# Proxmox 설정
proxmox_api_url = "https://your-proxmox:8006/api2/json"
proxmox_user = "terraform@pve"
proxmox_password = "your-password"
proxmox_tls_insecure = true

# SSH 키
ssh_public_key = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIB..."

# VM 기본 설정
vm_count = 3
vm_name_prefix = "gpu-vm"
vm_cores = 8
vm_memory = 8192
vm_disk_size = 200

# PCI Device 패스스루 설정 (권장: Proxmox Resource Mappings)
# Datacenter -> Resource Mappings에서 GPU 등을 사전에 정의해 두고 매핑 ID를 사용하세요.
# 예: gpu0, gpu1 등. 매핑이 없는 VM은 빈 문자열 유지.
pci_mappings = [
  "gpu0",  # VM 1
  "gpu1",  # VM 2
  ""       # VM 3 (미사용)
]

# (백워드 호환) 개별 매핑 변수
pci_mapping_1 = "gpu0"
pci_mapping_2 = "gpu1"
pci_mapping_3 = ""

# PCI 디바이스 옵션
hostpci_pcie = true    # PCIe 모드 사용
hostpci_rombar = false # ROM BAR 비활성화 (GPU는 보통 false)

# 추가 디스크 설정 (옵션)
additional_disk_enabled = false
additional_disk_size = 500
additional_disk_storage = "local-lvm"
